const { ValidationError, where } = require("sequelize");
const db = require("../../database/models");

const userServices={};

userServices.login= async(user, password)=>{
        
}

module.exports= userServices;